name: TITAN-EMPIRE-V124-GHOST-PROTOCOL

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 9600

    steps:
      - name: 1. Core Engine Setup
        run: |
          Stop-Service -Name wuauserv, SysMain -Force
          pip install psutil pyautogui --quiet
          choco install googlechrome --accept-license -y

      - name: 2. Tailscale Auto-Login
        continue-on-error: true 
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile "ts.msi"
          Start-Process msiexec.exe -ArgumentList "/i", "ts.msi", "/quiet", "/norestart" -Wait
          $authKey = "tskey-auth-kYcM6WeyfR11CNTRL-9FQy2jXXfC6nPVvmruwuC66aU7SsCJz9h" 
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$authKey --hostname="titan-ghost-v124" --accept-routes
          Start-Sleep -Seconds 15

      - name: 3. Create RDP & Advanced Anti-Bot Script
        run: |
          $password = "Admin@Watch786"
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          if (!(Get-LocalUser -Name "RDP" -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
              Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          }

          $ghostScript = @"
          import os
          import time
          import subprocess
          import psutil
          import random
          import pyautogui

          links = [
              "https://youtu.be/oiyYa3IL2Rc?si=LnzHB2SnxLvotRp-",
              "https://youtu.be/OXuTuHQqB7s?si=hsLjg-WN4Kddl2en",
              "https://youtu.be/K9ihEWJn4Go?si=VMP02dzeGwZtH_G0",
              "https://youtu.be/z25lo2mogbk?si=RDQn25eKVGl6u8Dy",
              "https://youtu.be/ZkJlQ_q7x5Q?si=5vafU9AuYNTZcp5E"
          ]
          
          chrome_path = r'C:\Program Files\Google\Chrome\Application\chrome.exe'
          profile_base = r'C:\Users\Public\TitanGhostProfiles'

          def human_behavior():
              # AI logic to move mouse and scroll like a human
              try:
                  x, y = random.randint(100, 500), random.randint(100, 500)
                  pyautogui.moveTo(x, y, duration=0.5)
                  if random.random() > 0.5:
                      pyautogui.scroll(random.randint(-200, 200))
              except: pass

          def start_ghost_empire():
              print("[GHOST] Launching 25 Stealth Profiles...")
              for i in range(25):
                  url = links[i % len(links)]
                  p_dir = os.path.join(profile_base, f'Ghost_{i}')
                  
                  # Feature: Adding Anti-Bot Flags
                  flags = [
                      chrome_path, 
                      f'--user-data-dir={p_dir}', 
                      '--no-first-run', 
                      '--mute-audio', 
                      '--disable-blink-features=AutomationControlled',
                      '--window-size=500,450',
                      '--force-device-scale-factor=0.5',
                      '--lang=en-US',
                      url
                  ]
                  subprocess.Popen(flags)
                  time.sleep(6) # Increased delay to avoid IP spike
                  human_behavior()

          # ETERNAL CYCLE WITH HUMAN LOGIC
          while True:
              for proc in psutil.process_iter(['name']):
                  if proc.info['name'] == 'chrome.exe': proc.kill()
              time.sleep(5)
              
              start_ghost_empire()
              
              # Cycle loop with active human movement every 2 minutes
              for _ in range(120): # 4 hours total
                  human_behavior()
                  time.sleep(120)
          "@
          
          $ghostScript | Out-File -FilePath "C:\Users\Public\Desktop\TITAN_GHOST.py" -Encoding ascii
          "@echo off`npython C:\Users\Public\Desktop\TITAN_GHOST.py`npause" | Out-File -FilePath "C:\Users\Public\Desktop\START_GHOST.bat" -Encoding ascii

      - name: 4. Final Instruction
        run: |
          Write-Host "############################################"
          Write-Host "V124 GHOST PROTOCOL READY!"
          Write-Host "TIP: RDP login ke baad manually 1-2 profiles mein VPN"
          Write-Host "connect kar dein, baaki profiles automatically trace hongi."
          Write-Host "############################################"
          Start-Sleep -Seconds 9000
