name: TITAN-V131-OMNI-BYPASS

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 9600

    steps:
      - name: 1. Deep System Hardening
        run: |
          # Audio aur फालतू services ko band karna resources bachane ke liye
          Stop-Service -Name wuauserv, SysMain, AudioEndpointBuilder, audiosrv -Force
          pip install psutil pyautogui numpy --quiet
          choco install googlechrome --accept-license -y

      - name: 2. Tailscale Secure Tunnel (Auth Key)
        continue-on-error: true 
        run: |
          $authKey = "tskey-auth-kYcM6WeyfR11CNTRL-9FQy2jXXfC6nPVvmruwuC66aU7SsCJz9h" 
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile "ts.msi"
          Start-Process msiexec.exe -ArgumentList "/i", "ts.msi", "/quiet", "/norestart" -Wait
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$authKey --hostname="titan-omni-v131" --accept-routes

      - name: 3. Create THE OMNI-BYPASS Master Script
        run: |
          $password = "Admin@Watch786"
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          if (!(Get-LocalUser -Name "RDP" -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
              Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          }

          $omniScript = @"
          import os
          import time
          import subprocess
          import psutil
          import random
          import pyautogui

          # --- ADVANCED OMNI-BYPASS LINKS ---
          # 1. Embed API + 2. Social Referrer + 3. Loop Logic
          raw_video_ids = ["0", "1", "2", "3", "4"] 
          
          # AI Logic: Converting to Ghost Links with WhatsApp Referrer Spoofing
          links = [f"https://www.youtube.com/embed/{vid}?autoplay=1&mute=1&loop=1&playlist={vid}&rel=0&controls=0&origin=https://web.whatsapp.com" for vid in raw_video_ids]
          
          chrome_path = r'C:\Program Files\Google\Chrome\Application\chrome.exe'
          profile_base = r'C:\Users\Public\TitanOmniProfiles'

          def launch_omni_session():
              print("[AI-OMNI] Deploying Stealth Fingerprints...")
              for i in range(25):
                  url = links[i % len(links)]
                  p_dir = os.path.join(profile_base, f'OmniUser_{i}')
                  
                  # Hybrid User-Agents (Changing every launch)
                  ua = random.choice([
                      "Mozilla/5.0 (Linux; Android 13; SM-X906B) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
                      "Mozilla/5.0 (iPad; CPU OS 17_1 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/17.1 Mobile/15E148 Safari/604.1",
                      "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/121.0.0.0 Safari/537.36"
                  ])

                  flags = [
                      chrome_path, 
                      f'--user-data-dir={p_dir}', 
                      f'--user-agent={ua}',
                      '--no-first-run', 
                      '--mute-audio', 
                      '--disable-blink-features=AutomationControlled', # Crucial for Bot Bypass
                      '--window-size=400,300',
                      '--force-device-scale-factor=0.5',
                      '--disable-infobars',
                      '--no-sandbox',
                      '--disable-gpu',
                      url
                  ]
                  subprocess.Popen(flags)
                  # AI randomized interval to prevent IP pattern detection
                  time.sleep(random.uniform(6, 10))

          while True:
              # Kill old processes and clear memory
              for proc in psutil.process_iter(['name']):
                  if proc.info['name'] == 'chrome.exe':
                      try: proc.kill()
                      except: pass
              time.sleep(5)
              
              launch_omni_session()
              print("[SYSTEM] Omni-Bypass Session Active. AI Monitoring...")
              
              # AI Human Mimicry Loop (Moves mouse to keep session alive)
              for _ in range(240): # 4 Hours
                  try:
                      pyautogui.moveRel(random.randint(-5, 5), random.randint(-5, 5))
                  except: pass
                  time.sleep(60)
          "@
          
          $omniScript | Out-File -FilePath "C:\Users\Public\Desktop\TITAN_OMNI.py" -Encoding ascii
          
          # Advance AI .bat with Command-Line Proxy logic
          $batContent = "@echo off`ntitle TITAN OMNI-BYPASS COMMANDER`ncolor 0D`necho [AI] Flushing Network Caches...`nipconfig /flushdns >nul`necho [AI] Bypassing Google Bot-Detection Sensors...`nnet stop audiosrv /y >nul 2>&1`necho [AI] Activating Social Referrer Spoofing...`npython C:\Users\Public\Desktop\TITAN_OMNI.py`npause"
          $batContent | Out-File -FilePath "C:\Users\Public\Desktop\START_OMNI.bat" -Encoding ascii

      - name: 4. Deployment Complete
        run: |
          Write-Host "############################################"
          Write-Host "V131 OMNI-BYPASS ENGINE IS LIVE!"
          Write-Host "REFERRER: WhatsApp Web (Spoofed)"
          Write-Host "BOT-BYPASS: AutomationControlled=Disabled"
          Write-Host "Just run START_OMNI.bat and watch the magic!"
          Write-Host "############################################"
          Start-Sleep -Seconds 9000
