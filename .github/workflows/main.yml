name: TITAN-V132-FINAL-GHOST

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 9600

    steps:
      - name: 1. Deep System Cleaning
        run: |
          Stop-Service -Name wuauserv, SysMain, AudioEndpointBuilder, audiosrv -Force
          pip install psutil pyautogui --quiet
          choco install googlechrome --accept-license -y

      - name: 2. Tailscale Secure Tunnel
        continue-on-error: true 
        run: |
          $authKey = "tskey-auth-kYcM6WeyfR11CNTRL-9FQy2jXXfC6nPVvmruwuC66aU7SsCJz9h" 
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile "ts.msi"
          Start-Process msiexec.exe -ArgumentList "/i", "ts.msi", "/quiet", "/norestart" -Wait
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$authKey --hostname="titan-final-v132" --accept-routes

      - name: 3. Create THE FINAL GHOST Script (With Fixed Links)
        run: |
          $password = "Admin@Watch786"
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          if (!(Get-LocalUser -Name "RDP" -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
              Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          }

          $finalScript = @"
          import os
          import time
          import subprocess
          import psutil
          import random

          # --- YOUR CHANNEL LINKS (FIXED & SECURED) ---
          # Humne aapke 5 links ko Embed API format mein convert kar diya hai
          raw_ids = ["0", "1", "2", "3", "4"] 
          
          # AI Logic: Social Referrer (WhatsApp) + Embed API + Autoplay + Mute
          links = [f"https://www.youtube.com/embed/{id}?autoplay=1&mute=1&loop=1&playlist={id}&origin=https://web.whatsapp.com" for id in raw_ids]
          
          chrome_path = r'C:\Program Files\Google\Chrome\Application\chrome.exe'
          profile_base = r'C:\Users\Public\TitanFinalGhost'

          def launch_titan():
              print("[GHOST] Launching 25 Protected Windows with WhatsApp Referrer...")
              for i in range(25):
                  url = links[i % len(links)]
                  p_dir = os.path.join(profile_base, f'User_{i}')
                  
                  # Randomized Stealth Identity
                  ua = random.choice([
                      "Mozilla/5.0 (iPad; CPU OS 17_0 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/17.0 Mobile/15E148 Safari/604.1",
                      "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/121.0.0.0 Safari/537.36"
                  ])

                  flags = [
                      chrome_path, 
                      f'--user-data-dir={p_dir}', 
                      f'--user-agent={ua}',
                      '--no-first-run', 
                      '--mute-audio', 
                      '--disable-blink-features=AutomationControlled', # Bypasses "Sign in" prompt
                      '--window-size=400,300',
                      '--force-device-scale-factor=0.5',
                      '--autoplay-policy=no-user-gesture-required',
                      url
                  ]
                  subprocess.Popen(flags)
                  time.sleep(random.uniform(7, 10)) # Human-like launch delay

          while True:
              for proc in psutil.process_iter(['name']):
                  if proc.info['name'] == 'chrome.exe':
                      try: proc.kill()
                      except: pass
              time.sleep(10)
              launch_titan()
              print("[SUCCESS] Session Started. AI Cycle active for 4 hours.")
              time.sleep(14400) 
          "@
          
          $finalScript | Out-File -FilePath "C:\Users\Public\Desktop\TITAN_FINAL.py" -Encoding ascii
          "@echo off`ntitle TITAN FINAL`nnet stop audiosrv /y >nul 2>&1`npython C:\Users\Public\Desktop\TITAN_FINAL.py`npause" | Out-File -FilePath "C:\Users\Public\Desktop\START_FINAL.bat" -Encoding ascii

      - name: 4. Deployment Instruction
        run: |
          Write-Host "V132 FINAL DEPLOYED!"
          Write-Host "LINKS STATUS: INTEGRATED & SECURED"
          Write-Host "BOT BYPASS: WHATSAPP REFERRER ACTIVE"
          Start-Sleep -Seconds 9000
